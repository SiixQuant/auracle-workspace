---
description: Scan recent arXiv research and synthesize strategy ideas you can actually backtest here
---

First call the `data_coverage` tool to see what this install can actually backtest — the ingested symbols, their asset classes, and the date range. Then scan recent arXiv papers with the `research_scan` tool for effects that fit THAT universe: choose categories and keywords matching the asset classes you actually have, not a fixed theme. Return the most relevant papers with their abstracts, then synthesize at least three concrete, measurable strategy ideas whose universe is drawn ONLY from the backtestable symbols, and write them up as a markdown research note. If a compelling idea needs instruments this install doesn't have, say so and note what would need ingesting (`ingest_historical_bars` covers US equities/ETFs) rather than proposing something that can't be tested here.
